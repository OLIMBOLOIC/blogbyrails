<%= form_for([@post, @post.commentaires.build]) do |f|  %>
 <p>
            <%= f.label :Nom %> <br>
            <%= f.text_field :nom %> <br>
        </p>
        <p>
            <%= f.label :Commentaire %> <br>
            <%= f.text_area :commentaire %> <br>
        </p>
    <p>
            <%= f.submit :Commenter %> <br>
        </p>

<% end %>